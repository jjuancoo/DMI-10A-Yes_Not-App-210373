name: yes_no_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  dio: ^5.7.0
  flutter:
    sdk: flutter
  provider: ^6.0.5
  flutter_launcher_icons: ^0.14.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # Define assets if you are using them
  assets:
    - assets/speech-bubble.png

flutter_icons:
  android: true
  ios: true
  image_path: "assets/speech-bubble.png"
